
name: Pipeline

on:
  push:
    branches:
      - master
  pull_request:
    branches: [master]
    types: [opened, synchronize]

jobs:
  simple_deployment_pipeline:
    runs-on: ubuntu-18.04
    steps:
      - name: Check out the sources
        uses: actions/checkout@v2

      # - name: Install Node.js
      #   uses: actions/setup-node@v1
      #   with:
      #     node-version: '12.x'

      # - name: Install dependencies
      #   run: npm install

      # - name: Lint the code
      #   run: npm run eslint

      # - name: Create deliverables for production
      #   run: npm run build

      # - name: Run unit tests
      #   run: npm test

      # - name: Run end-to-end tests
      #   uses: cypress-io/github-action@v2
      #   with:
      #     start: npm run start-prod
      #     wait-on: "http://localhost:5000"
      #     command: npm run test:e2e
      #     spec: cypress/integration/*.js

      # - name: Increase the version and create a tag
      #   if: ${{ github.event_name == 'push' && !contains(join(github.event.commits.*.message, ' '), '#skip') }}
      #   uses: anothrNick/github-tag-action@eca2b69f9e2c24be7decccd0f15fdb1ea5906598
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #     DEFAULT_BUMP: patch

      # - name: Deploy to Heroku
      #   if: ${{ github.event_name == 'push' && !contains(join(github.event.commits.*.message, ' '), '#skip') }}
      #   uses: akhileshns/heroku-deploy@79ef2ae4ff9b897010907016b268fd0f88561820
      #   with:
      #     heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
      #     heroku_app_name: "pokedex-al"
      #     heroku_email: "aleksi.lukkarinen@gmail.com"
      #     healthcheck: "https://pokedex-al.herokuapp.com/health"
      #     checkstring: "ok"
      #     rollbackonhealthcheckfailed: true

      - name: Notify in Slack about a successful deployment to Heroku
        uses: 8398a7/action-slack@047b09b154480ed39076984b64f324fff010d703
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        with:
          status: custom
          author_name: simple_deployment_pipeline
          job_name: simple_deployment_pipeline
          text: A new Pokedex version (${{env.AS_COMMIT}}) deployed to https://pokedex-al.herokuapp.com/
          fields: repo,commit,message,author
          mention: here
          if_mention: failure,cancelled
          custom_payload: |
            {
              attachments: [{
                color: '${{ job.status }}' === 'success' ? 'good' : '${{ job.status }}' === 'failure' ? 'danger' : 'warning',
                text: `${env.AS_WORKFLOW}\n${env.AS_JOB} (${env.AS_COMMIT}) of ${env.AS_REPO}@${env.AS_REF} by ${env.AS_AUTHOR} ${{ job.status }} in ${env.AS_TOOK}`,
              }]
            }
        if: always()

#        if: ${{ github.event_name == 'push' && !contains(join(github.event.commits.*.message, ' '), '#skip') && job.status == 'success' }}
